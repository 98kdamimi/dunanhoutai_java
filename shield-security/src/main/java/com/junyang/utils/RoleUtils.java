package com.junyang.utils;

public class RoleUtils {
    /**
     * 判断first的角色是否能创建second的角色
     * @return 如果first的角色能创建second的角色，返回true
     */
    public static boolean firstRoleCanCreateSecond(Integer first, Integer second){
        return true;
    }
}
